<%= form_for(descussion) do |f| %>
  <% if descussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(descussion.errors.count, "error") %> prohibited this descussion from being saved:</h2>

      <ul>
      <% descussion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
